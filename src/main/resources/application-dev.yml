# Local/demo only. Do not use this profile in production.
jwt:
  secret: DevOnlySecretKeyThatIsAtLeastThirtyTwoBytesLong!!

app:
  seed:
    enabled: true
    admin-password: ${SEED_ADMIN_PASSWORD:Admin@123}
    user-password: ${SEED_USER_PASSWORD:User@123}
